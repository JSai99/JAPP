# 自動部署 GitHub Pages：main 有更新就重新發布整個工具箱
# 網址：https://jsai99.github.io/JAPP/
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:   # 也可以在 Actions 頁面手動觸發

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages（首次執行會自動啟用 Pages）
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .   # 純靜態網站，整個 repo 直接發布

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
